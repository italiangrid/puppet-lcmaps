<%-
@pools.each do | pool |
  @vo_name=pool['vo']
  @group=pool['group']
  @capability=pool['capability']
  if @capability == nil
    @capability = "NULL"
  end
  @roles=pool['roles']
  @roles.each do | role |
-%>
"/<%=@vo_name%>/Role=<%=role%>/Capability=<%=@capability%>" <%=@group%>
"/<%=@vo_name%>" <%=@group%>
<%- 
  end
end
-%>
